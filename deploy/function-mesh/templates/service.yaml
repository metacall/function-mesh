apiVersion: v1
kind: Service
metadata:
  name: {{ include "function-mesh.fullname" . }}
  labels:
    {{- include "function-mesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "function-mesh.selectorLabels" . | nindent 4 }}
